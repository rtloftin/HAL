"""
Defines a reinforcement learning agent which
uses the TRPO algorithm for learning.

TRPO may not be feasible to implement in tensorflow, so
we should save its implementation for a time when we are certain
it is needed, and we have already gotten results against other
learning algorithms.
"""